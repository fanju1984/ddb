std::string selectSql1="select * from Student;";
    std::string selectSql2="select Course.name from Course;";
    std::string selectSql3="select * from Course where credit_hour>2 and location='CB-3';";
    std::string selectSql4="select Exam.course_id,Exam.mark from Exam;";
    std::string selectSql5="select Course.name, Course.credit_hour, Teacher.name from Course, Teacher where Course.teacher_id=Teacher.id and Course.credit_hour>2 and Teacher.title=3;";
    std::string selectSql6="select Student.name,Exam.mark from Student,Exam where Student.id=Exam.student_id;";
    std::string selectSql7="select Student.id,Student.name,Exam.mark,Course.name from Student,Exam,Course where Student.id=Exam.student_id and Exam.course_id=Course.id and Student.age>26 and Course.location<>'CB-3';";
    
    std::string createSql1="create table Student(id int key,name char(25),sex char(1),age int,degree int);";
    std::string createSql2="create table Teacher(id int key,name char(25),title int);";
    std::string createSql3="create table Course(id int key,name char(80),location char(8),credit_hour int,teacher_id int);";
    std::string createSql4="create table Exam(student_id int,course_id int,mark int);";
    
    std::string insertSql1="insert into Student values(1000001,'xiao ming','M',20,1);";
    std::string insertSql2="insert into Teacher values(2900010,'Santa Claus',2);";
    std::string insertSql3="insert into Teacher values(2000001,'St. Nicholas',1);";
    std::string insertSql4="insert into Course values(3900001,'Defence Against Dark Arts','CB-3',4,200001);";
    std::string insertSql5="insert into Exam values(1900001,3900001,96);";

    std::string fragSql1="frag Student id < 105000 db1 id >= 105000 and id < 110000 db2 id >= 110000 db3;";
    std::string fragSql2="frag Teacher id < 201000 and title <> 3 db1 id < 201000 and title = 3 db2 id >= 201000 and title <> 3 db3 id >= 201000 and title = 3 db4;";
    std::string fragSql3="frag Course (id,name) db1 (id,location,credit_hour,teacher_id) db2;";
    std::string fragSql4="frag Exam student_id < 107000 and course_id < 301200 db1 student_id < 107000 and course_id >= 301200 db2 student_id >= 107000 and course_id < 301200 db3\n"
                         "student_id >= 107000 and course_id >= 301200 db4;";

    std::string delSql1="delete from Teacher where title = 1;";
    std::string delSql2="delete from Teacher where id >= 2900010 and title = 2;";
    std::string delSql3="delete from Student where id = 1000001;";
    std::string delSql4="delete from Course where location = 'CB-3';";
    std::string delSql5="delete from Exam;";
